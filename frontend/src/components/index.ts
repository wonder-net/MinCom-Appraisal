/**
 * Shared UI components for the MINCOM Appraisal Platform.
 *
 * This module will contain reusable, presentation-focused components
 * that are shared across multiple features, including:
 * - Layout components (AppShell, Sidebar, Header, Footer)
 * - Form primitives (FormField, FormError, ValidationMessage)
 * - Data display (DataTable, StatusBadge, ScoreIndicator)
 * - Feedback (LoadingSpinner, EmptyState, ErrorBoundary)
 * - Navigation (Breadcrumbs, TabNav)
 *
 * Built on top of shadcn/ui primitives with Tailwind CSS styling.
 * All components must be accessible (ARIA labels, keyboard navigation).
 */
