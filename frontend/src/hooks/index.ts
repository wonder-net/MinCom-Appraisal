/**
 * Custom React hooks for the MINCOM Appraisal Platform.
 *
 * This module will contain reusable hooks, including:
 * - useAuth — access authentication context (user, roles, tokens)
 * - useAppraisal — fetch and manage a single appraisal
 * - useDebounce — debounce rapidly changing values
 * - useMediaQuery — responsive breakpoint detection
 * - usePagination — offset-based pagination state management
 * - useFormValidation — client-side form validation helpers
 *
 * All hooks follow the "use" prefix convention and are placed
 * here for cross-feature reuse.
 */
