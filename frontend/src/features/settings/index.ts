/**
 * Settings feature module for the MINCOM Appraisal Platform.
 *
 * Contains:
 * - Account Settings page (password change + MFA management)
 *
 * Route: /settings/account — accessible to all authenticated users.
 */

export { AccountSettings } from "./pages/AccountSettings";
